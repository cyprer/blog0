# 第二周周结
  本周完成了后端代码完善与 Redis 问题排查，进行了多次技术选型尝试（minimax→codex→astrbot→OpenClaw，nanobot→nanocode），学习 langchain、smolagent、playwright 等技术。规划了医疗助手和爬虫 AI（小红书文章生成）两个项目方向。工作推进受 mentor 较忙缺乏指导影响，部分项目（如知光项目、2022 项目迁移）遇到阻碍。周末 C 盘空间告急，日常有些迷茫摇摆。

## 4.8
  周二，晴天。完善后端逻辑和错误处理机制，测试 Redis 时遇到读取问题（本地可读但启动项目后不可读）。代码遇到表关联关系问题差点出大错。决定从 minimax 切换到 codex 尝试。发现之前用 RedisTemplate 实现的鱼类服务，而 Redis2 实现类已有 hash 操作，只是方法名不同导致未注意到。晚间弃用 openclaw/qclaw 改用 astrbot，感觉 astrbot 也不太好用，最终又换回 OpenClaw。

## 4.9
  周四，晴天。尝试实现鱼类列表滚动效果失败，mentor 表示三个就够了。中午写力扣。下午研究 smolagent 想构建多 agent 代码，卡在配置上。看了斯坦福小镇论文项目但感觉面试官不感兴趣，又考虑给知光项目扩展 AI 功能但配置太麻烦。晚上研究 nanobot 和 nanocode，觉得项目太大太复杂，决定改学 nanocode，但接入模型的方式很奇怪，很累。

## 4.10
  周五，晴天。mentor 审批代码，修改问题。下午看 langchain、nanocode 等，没新任务。下班买奶茶，晚上打王者。

## 4.11
  周六，晴天。中午才醒。用 langchain 构建 agent 玩，看了大概又迷茫了，不知道做啥。又打王者，决定做一个医疗助手，感觉 AI 适合这个场景。

## 4.12
  周日，晴天。还是中午醒的。晚上思考 AI 项目方向，考虑在知光项目里用 langchain4j。尝试启动知光项目启动不了，折腾一晚上终于搞定。C 盘空间告急（只剩 40G），删了半天也没清出多少。

## 4.13
  周一，晴天。mentor 让改后端代码后修改完成。mentor 很忙没空回复，自己看 AI。决定做一个爬虫 AI：爬取微博热搜数据生成文章发小红书。晚上开始搞这个，mentor 又让跑 2022 年的项目，不在公司没法跑，折腾半天。

## 4.14
  周一，晴天。修改鱼类重复问题，加了去重处理（原来是远程自己插的重复数据）。中午写力扣，下午学习开源小红书内容生成项目，了解到 agent 可用 playwright 和 skills 进行浏览器操作，正好适配小红书 AI 项目。mentor 安排将 2022 项目验证码验证迁移到深圳项目。晚上摸索 openclaw 的 playwright 功能，被叫去打王者，打完继续摸索，测试续火花功能写成自动任务失败。
